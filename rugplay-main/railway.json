{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd website && node build",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100
  }
}
